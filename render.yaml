services:
  - type: web
    name: doudizhu-lan-html
    runtime: node
    plan: free
    autoDeployTrigger: commit
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
